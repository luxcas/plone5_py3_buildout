[users]
effective-user = plone
user = admin
password = admin

[hosts]
instance = 0.0.0.0

[ports]
instance = 8400

[supervisor-settings]
user = admin
password = admin

[blobstorage]
directory = ${buildout:directory}/var/blobstorage

[tmp]
directory = /tmp
